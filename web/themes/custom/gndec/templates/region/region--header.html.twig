{#
/**
 * @file
 * Theme implementation to display the header region.
 */
#}
{{ attach_library('gndec/header-styling') }}
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {{ content }}
  </div>
{% endif %}
