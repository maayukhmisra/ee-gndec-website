{#
/**
 * @file
 * Theme implementation to display the footer region.
 */
#}
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
    'footer-blocks-grid',
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {{ content }}
  </div>
{% endif %}
